/**
 * This module contains all the configuration for the build process
 */
module.exports = {
    /**
     * dev_dir Directory is where our projects are compiled for development.
     * prod_dir Directory is where our projects are compiled.
     */
    src_dir: 'src',
    dev_dir: 'build',
    prod_dir: 'bin'

};
